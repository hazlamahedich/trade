#!/bin/bash
set -eu

ST1:deploy backend to Railway
staging
"}
set -e
echo "To deploy to Railway: $railway login"
railway whoami" || "railway run --app deploy"
rails/handle_staging commit" errors
 set to staging environment variables:
 defaults to "production" }
fi
